{
  "name": "hospital-asset-server",
  "version": "1.0.0",
  "description": "Hospital Asset Tracker — API and Dashboard server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "cd ../web_dashboard && npm install && npm run build && xcopy /E /I /Y dist ..\\server\\public",
    "build:unix": "cd ../web_dashboard && npm install && npm run build && cp -r dist/* ../server/public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
